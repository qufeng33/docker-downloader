/**
 * ====================================
 * 共享类型总导出
 * ====================================
 *
 * 统一导出所有共享类型，方便其他模块导入
 */

// 基础接口类型（所有进程可用）
export * from './base'

// IPC 通信相关类型
export * from './ipc'

// API 类型和 Window 扩展（渲染进程用）
export * from './api'

// DTO 类定义在主进程目录中，不在共享类型中导出
